/*
 * C 实战练习场（学完翁恺课程后的多文件工程模板）
 * 用法：
 *   1. 打开 VS Code（这个文件夹）
 *   2. 按 Ctrl+Shift+P → "CMake: Configure" → 选 default preset
 *   3. F7 构建，F5 调试
 */
#include <stdio.h>

int main(void)
{
    printf("Hello, C!\n");
    return 0;
}
